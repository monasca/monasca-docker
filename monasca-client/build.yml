repository: fest/client
variants:
  - tag: pike-latest
    aliases:
      - :pike-{date}-{time}
    args:
      CLIENT_BRANCH: pike-latest
      PYTHON_VERSION: '2'
      TIMESTAMP_SLUG: pike-latest
      CONSTRAINTS_BRANCH: pike-latest
 
