---
# TODO should this be a plain pod? it should only be run once...
apiVersion: v1
kind: ReplicationController
metadata:
  name: monasca-thresh
  namespace: monitoring
spec:
  replicas: 1
  selector:
    app: monasca-thresh
  template:
    metadata:
      labels:
        app: monasca-thresh
    spec:
      containers:
        - name: monasca-thresh
          image: rbrndt/monasca-thresh:latest
          volumeMounts:
            - name: thresh-config-volume
              mountPath: /config
          env:
            - name: NIMBUS_ADVERTISED_HOSTNAME
              value: nimbus
      volumes:
        - name: thresh-config-volume
          configMap:
            name: monasca-thresh-config
