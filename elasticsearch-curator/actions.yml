curator_actions:
  - {
      delete_by: age,
      description: 'Delete indices older than 60 days',
      deleted_days: 60,
      disable: False
    }
  - {
      delete_by: space,
      description: 'Delete oldest indices over 5 GB',
      deleted_space: 5,
      disable: True
    }

curator_excluded_index:
  - {
      index_name: .kibana,
      exclude: True
    }
