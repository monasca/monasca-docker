curator_actions:
  - {
      delete_by: age,
      description: 'Delete indices older than 60 days',
      deleted_days: 60,
      disable: False
    }

curator_excluded_index:
  - {
      index_name: .kibana,
      exclude: True
    }
